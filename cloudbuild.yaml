# Google Cloud Build configuration
# Triggers on: main branch (default) or master branch
steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/birefnet-api', '.']
    timeout: 1200s

  # Push the image to Google Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/birefnet-api']

  # Deploy to Cloud Run with extended startup timeout
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'birefnet-api' 
      - '--image=gcr.io/$PROJECT_ID/birefnet-api'
      - '--region=us-central1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--memory=8Gi'
      - '--cpu=4'
      - '--timeout=900s'
      - '--max-instances=10'
      - '--port=8080'
      - '--set-env-vars=PORT=8080,OMP_NUM_THREADS=4,TORCH_THREADS=4'
      - '--execution-environment=gen2'
      - '--cpu-boost'
      - '--min-instances=1'
      - '--concurrency=4'

# Build configuration - optimized for faster builds
options:
  machineType: 'E2_HIGHCPU_32'
  diskSizeGb: 200
  logging: CLOUD_LOGGING_ONLY

timeout: 1800s